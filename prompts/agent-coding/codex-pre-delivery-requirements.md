---
title: 交付前确认：3 方案 + 验收清单，确认前不写码
category: agent-coding
source: https://x.com/SevenXL77/status/2088455155498750386
ran_on: herdr prompt-lab / prompt-agy
ran_at: 2026-09-04
model: gemini-3.8-flash-high (agy)
input_status: rebuilt_from_ab_excerpt
excerpt_a: runs/ab/batch-b/confirm-A.md
excerpt_b: runs/ab/batch-b/confirm-B.md
gate_complete: true
gate_traceable: true
gate_safe: true
gate_one_pass: true
---

# 交付前确认：3 方案 + 验收清单，确认前不写码

先给三个方向方案和可验证验收清单，确认前不许写代码。

## 原始问题

个人本地待办 Web 需求下，模型常直接交可粘贴实现。这次测第一轮是否停在方案+验收。

## 给模型的输入

这段输入根据 A/B 摘录重建，不是官方逐字原文。

```text
我要做个个人本地待办 Web（单页、无账号）。先不要写代码：给 3 个不同方向方案，并整理可验证验收清单，等确认后再开发。
```

## 复制这条 Prompt

把上面的输入和下面这段 Prompt 放进同一条消息，再发送。

```text
我要做个[某某项目]。先不要写代码，第一步确认需求，向我提供 3 个不同方向的企业级别决定方案，非关键细节请自行做合理假设，简单问题或者重复问题你可以自行确认，关键决定或者方案由我来选择确认。

询问结束后，把需求整理成一份准确率和精准度足够高的精简、可验证的markdown格式的验收清单，覆盖项目背景、核心流程、具体架构、异常情况、骨架屏状态、加载状态、以及适配哪些终端。

等我确认后再开始开发。完成后必须实际运行项目，按照验收清单逐项验证并汇报结果；未通过的项目继续修改，直到达到验收标准。
```

## 跑完会差在哪

### A：裸跑

A 仅中性「请完成任务」。

> # 个人本地待办 Web 应用构建方案 (SPA·无账户)
>
> ---
>
> ## 一、方案概览
> 本方案遵循 **KISS 原则**，构建一个完全运行在浏览器本地的单页面待办应用（Single Page Application）。
> - **零外部服务依赖**：无需后端服务器、无需数据库、无需用户账号注册系统。
> - **纯本地持久化**：使用浏览器原生 `localStorage` 自动保存所有数据，刷新或重启浏览器数据不丢失。
> - **即开即用**：采用单文件 HTML（内嵌样式与交互脚本），下载后直接在浏览器中双击打开即可使用。
>
> ---
>
> ## 二、功能特性
> …

### B：加上 Prompt

> # 个人本地待办 Web (SPA·无账号) 方案决策与验收规范
>
> > **阶段状态**：需求与方案确认阶段。**本阶段不包含任何实现代码**。待您选定架构方向并确认验收清单后，正式启动代码实现。
>
> ---
>
> ## 一、三个不同方向的企业级架构方案（请选择其一）
>
> 为满足“单页、无账号、个人本地运行”的需求，同时具备企业级代码规范与可维护性，提供以下 3 个架构演进方向供您决策：
>
> ### 方案 1：零构建原生现代轻量级（Vanilla Modern ESM + Web Components）
> * **定位**：极致纯粹、零构建工具链（No-Build）、开箱即用。
> …

A 直接交付实现（含可粘贴页面）；B 给 3 个架构方向 + 覆盖骨架/加载/错误态的验收清单，声明本阶段无代码。

## 什么时候别用

需求与方案已锁定、进入实现冲刺时，不要再强制停在确认门。

## 人要检查什么

是否真的无代码；验收清单章节是否覆盖原文要求。

## 四维评测

| 维度 | A：裸跑 | B：加上 Prompt |
| --- | --- | --- |
| 结构 | 直接交付实现（含可粘贴页面代码） | 3 个架构方向 + 验收清单，等选型 |
| 约束 | 违反「先不要写代码」 | 遵守；正文声明本阶段无代码 |
| 胡编 | 无 | 无 |
| 可执行性 | 能跑但跳过确认 | 可给人拍板后开发 |
